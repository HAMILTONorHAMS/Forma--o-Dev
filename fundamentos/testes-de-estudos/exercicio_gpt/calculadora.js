
/// )01- Calculadora simples

// const soma = (a,b) => a + b;
// console.log(soma(10,10))

// const sub = (a,b) => a - b;
// console.log(sub(10,10))

// const multi = (a,b) => a * b;
// console.log(multi(10,10))

// const div = (a,b) => a / b;
// console.log(div(10,10))

const calcular = (a,b) => {
     const soma = a + b
     const subtracao = a - b
     const multi = a * b
     const div = a / b     
     console.log(soma, subtracao, multi, div)
}
     calcular(10,10)
